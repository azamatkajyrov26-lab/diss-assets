# ЗАДАНИЕ: достроить дашборд прогнозирования внутри LMS-проекта и снять скриншоты

## ВАЖНО — границы задачи
- **НЕ трогать диссертацию.** Никаких docx, никакого текста диссертации — этого файла тут нет и он не нужен.
- Работаем ТОЛЬКО внутри проекта дашборда: правим/создаём HTML-страницы и делаем скриншоты.
- Результат: готовые HTML-страницы + свежие PNG-скриншоты в папке. Всё.

## КОНТЕКСТ
Это аналитический дашборд прогнозирования академического риска студентов (модуль «AnalyticsDashboard»
для LMS колледжа АПЕК ПетроТехник). Скачай файлы:

`git clone https://github.com/azamatkajyrov26-lab/diss-assets`

- **Существующий дашборд (расширять ПОВЕРХ, сохраняя стиль/шапку):**
  `dashboard/index.html` — страница overview, но с НЕВЕРНЫМИ демо-числами
  `dashboard/student-detail.html` — страница студента, тоже демо-числа
- **Канонические метрики:** `data/metrics.json`
- **Старые demo-скрины (для сверки стиля, НЕ как результат):** `screenshots/dashboard.png`, `screenshots/student-detail.png`

## РЕАЛЬНЫЕ ЧИСЛА (использовать везде вместо демо)
- Активных студентов: **247** (из 436 в 18 группах)
- В группе риска: **72** (≈**29%**)
- Новых в риске за 30 дней: **8**
- Победитель-модель: **LightGBM**, F1=**0.927**, ROC-AUC=**0.966**, Acc=**0.960**
- Сравнение моделей: RandomForest F1=0.900/AUC=0.978 · GradientBoosting 0.878/0.942 · MLP 0.865/0.961 · LogReg 0.850/0.952
- Топ-10 признаков: reading_time_ratio, assignment_mean_score, quiz_mean_score, quiz_time_mean, baseline_gpa,
  session_duration_std, attendance_ratio, scroll_max_depth, hesitation_score, assignment_ontime_ratio
- Confusion (LightGBM): TN=53, FP=2, FN=1, TP=19
- Период данных: 01.09.2025 – 31.03.2026
- Студенты — анонимные (ID вида АРК-089), без реальных имён.

## ЯЗЫК интерфейса
Казахский (как в существующих страницах). Тёмная/светлая тема — как в текущем дизайне.

## СТРАНИЦЫ (сделать 4 штуки)

1) **overview (правка `index.html`)** — заменить демо-числа на реальные: KPI 247 / 72 / 8 / ≈29%, победитель F1=0.927.
   Блоки: донат по уровням риска (жоғары/орта/төмен), bar по группам, тренд за 12 недель,
   важность признаков (топ-10), сравнение моделей по F1.

2) **students-list (СОЗДАТЬ `students-list.html`)** — таблица всех студентов, сорт по вероятности риска.
   Колонки: ID (аноним), топ, орташа балл, қатысу %, соңғы белсенділік, тәуекел ықтималдығы со светофором 🟢🟡🔴.
   Сверху фильтры (мамандық, курс, тәуекел деңгейі) и кнопки экспорта Excel/CSV.

3) **student-detail (правка `student-detail.html`)** — реальные числа. Блоки: карточка студента, GPA-тренд,
   график LMS-активности, **SHAP waterfall** (қай факторлар тәуекелді арттырды/азайтты).

4) **action-center (СОЗДАТЬ `action-center.html`)** — лента карточек-рекомендаций для преподавателя:
   - «Студент X соңғы 10 күн LMS-ке кірмеген — оған жеке хат жіберіңіз»
   - «2В тобының 5 студенті тапсырманы уақытында тапсырмаған — ортақ ескерту жіберіңіз»
   - «3А тобының белсенділігі 20% төмендеді — топтық кеңес өткізіңіз»
   У каждой карточки кнопка действия (хат жіберу / хабарландыру).

## СКРИНШОТЫ
Снять каждую из 4 страниц (full page, ширина ~1440px, headless — playwright/puppeteer) и сохранить в **новую папку**:
```
screenshots/dashboard-new/
  01-overview.png
  02-students-list.png
  03-student-detail.png
  04-action-center.png
```

## ВЫХОД (deliverables)
- 4 HTML-страницы (2 правленые + 2 новые) с реальными числами.
- 4 свежих PNG в `screenshots/dashboard-new/`.
- Короткий отчёт: что поправлено, что создано.

## НЕЛЬЗЯ
- Не редактировать диссертацию / docx (их тут нет).
- Не выдумывать числа — только из этого файла и `data/metrics.json`.
